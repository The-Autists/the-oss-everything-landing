<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <link rel="stylesheet" href="/global.css" />
    <title>The oss bot</title>
  </head>
  <body>
    <main>
      <slot />
    </main>
  </body>
</html>

<style is:global>
  body main {
    overflow-x: hidden;
  }

  body {
    color: var(--text);
    margin: 0;
    background: var(--background);
    width: 100%;
    height: 100%;
    font-family: "Poppins", sans-serif;
  }

  main {
    display: flex;
    flex-direction: column;
    gap: 128px;
  }

  @media (width < 1024px) {
    main {
      gap: 64px;
    }
  }



* {
  box-sizing: border-box;
}

a {
  cursor: pointer;
}

h1,
h2,
h3,
h4,
h5,
h6,
p {
  margin: 0;
}

h1 {
  font-size: 62px;
  font-weight: 700;
}

h2 {
  font-size: 48px;
  font-weight: 600;
  line-height: 48px;
}

h4 {
  color: var(--text-subheading);
  font-size: 24px;
  font-weight: 400;
}

h5 {
  color: var(--text-heading);
  font-size: 20px;
  font-weight: 600;
}

button {
  border: none;
  cursor: pointer;
}

.chip {
  border: 1px solid var(--accent);
  border-radius: 50px;
  width: fit-content;
  height: fit-content;
  padding: 6px 12px;
}

.chip p {
  color: var(--secondary);
  font-size: 18px;
  font-weight: 500;
}

.btn {
  background: var(--btn-background-primary);
  padding: 16px 20px;
  border-radius: 8px;
  font-size: 20px;
  transition: all 0.2s ease-in-out;
  text-decoration: none;
  color: #000;

  &:hover {
    box-shadow: 0 0 10px var(--btn-background-primary);
  }
}

.btn-secondary {
  background: transparent;
  color: var(--text);
  border: 2px solid var(--btn-background-secondary);

  &:hover {
    box-shadow: 0 0 10px var(--btn-background-secondary);
  }
}

.feature {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  gap: 128px;
}

.feature-reverse {
  flex-direction: row-reverse;
}

.card,
.feature img {
  border-radius: 8px;
  box-shadow: 0 0 24px var(--border);
  width: 500px;
}

.card {
  background: var(--card-background);
  display: flex;
  flex-direction: column;
  gap: 24px;
  padding: 12px 14px;
  align-items: start;
  justify-content: center;
  text-align: start;
  border-radius: 24px;
  border: 1px solid var(--border);
}

.card .chip {
  background: var(--chip-background);
  border: 1px solid var(--border);
}

.card .content {
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding: 0 12px;
}

.card .btn {
  font-size: 15px;
  padding: 12px 16px;
}

@media (width < 768px) {
  h1 {
    font-size: 36px;
  }

  h2 {
    font-size: 24px;
    line-height: 24px;
  }

  h4 {
    font-size: 16px;
  }

  h5 {
    font-size: 20px;
  }

  .btn {
    font-size: 14px;
  }

  .chip p {
    font-size: 16px;
  }

  .chip {
    padding: 4px 8px;
  }

  .feature-reverse {
    flex-direction: column;
  }

  .feature img,
  .feature .card {
    width: 100%;
  }
}

@media (width < 1024px) {
  .feature {
    flex-direction: column;
    gap: 32px;
    width: 100%;
  }
}

</style>
